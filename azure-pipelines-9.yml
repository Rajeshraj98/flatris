# Node.js with React
# Build a Node.js project that uses React.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
  - master

pool:
  vmImage: 'ubuntu-latest'

variables:
  NODE_OPTIONS: '--openssl-legacy-provider'

steps:
  - checkout: self
    fetchDepth: 1

  - task: NodeTool@0
    inputs:
      versionSpec: '20.19.4'
    displayName: 'Use Node.js 20.19.4'

  # Full clean install
  - script: |
      rm -rf node_modules package-lock.json
      npm cache clean --force
      
      # Install exact Next.js and matching webpack/tapable versions
      npm install next@14.2.5 --save-exact
      npm install webpack@5.89.0 tapable@2.2.1 --save-exact
      npm install
      npm dedupe
    displayName: 'Clean install dependencies'

  # Debug: show installed versions
  - script: |
      echo "Webpack versions in tree:"
      npm ls webpack || true
      echo "Tapable versions in tree:"
      npm ls tapable || true
    displayName: 'Check dependency versions'

  - script: rm -rf .next
    displayName: 'Clean Next.js cache'

  - script: npm run build
    displayName: 'Build project'

  # Only copy if build succeeded
  - task: CopyFiles@2
    condition: succeeded()
    inputs:
      SourceFolder: '.next'
      Contents: '**'
      TargetFolder: '$(Build.ArtifactStagingDirectory)/next-build'
    displayName: 'Copy Next.js build output'

  - task: PublishBuildArtifacts@1
    condition: succeeded()
    inputs:
      PathtoPublish: '$(Build.ArtifactStagingDirectory)/next-build'
      ArtifactName: 'next-build'
      publishLocation: 'Container'
    displayName: 'Publish build artifacts'
